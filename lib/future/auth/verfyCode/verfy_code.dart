import 'package:flutter/material.dart';

class VerfyCodeScrean extends StatelessWidget {
  const VerfyCodeScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('datavv')),
    );
  }
}
