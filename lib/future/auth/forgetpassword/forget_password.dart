import 'package:flutter/material.dart';

class ForgetPasswordScrean extends StatelessWidget {
  const ForgetPasswordScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
          child: Container(
              height: 500,
              width: 400,
              color: Colors.red,
              child: const Text('data'))),
    );
  }
}
